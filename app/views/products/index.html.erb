<div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(	https://cdn.pixabay.com/photo/2013/12/11/03/13/puzzle-226743_960_720.jpg)">
  HOME
</div>

<div class="container mt-5">
  <div class="row">
      <% @products.each do |product| %>
        <div class="col-md-4 col-6">
            <%= link_to product_path(product), class: "card-listing p-5" do %>
              <div class="card-product avatar_father">
                <% if product.photo.present? %>
                  <%= cl_image_tag product.photo.key, height: 300, width: 400, crop: :fill %>
                <% else %>
                  <%= image_tag 'space_baazar.png', height: 300, width: 400, crop: :fill %>
                <% end %>
              </div>
              <div class="d-flex justify-content-between align-items-center position-relative">
                <div class="card-product-infos">
                  <h2><%= product.name %></h2>
                  <span class="qtd">Qtd.<%= product.quantity %></span>
                </div>
                  <div >
                    <% if product.user.photo.attached? %>
                      <%= cl_image_tag product.user.photo.key, class: "avatar-card avatar", alt: "avatar-large" %>
                    <% else %>
                      <img class="avatar-card avatar" alt="avatar-large" src="https://kitt.lewagon.com/placeholder/users/arthur-littm" />
                  </div>
                <% end %>
              </div>
            <% end %>
        </div>
      <% end %>
  </div>
</div>
